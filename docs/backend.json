{
  "entities": {
    "UserProfile": {
      "title": "Perfil de Usuario",
      "description": "Representa el perfil de un usuario en el sistema.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "El nombre para mostrar del usuario."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "La dirección de correo electrónico del usuario."
        },
        "role": {
          "type": "string",
          "description": "El rol del usuario en el sistema.",
          "enum": [
            "admin",
            "technician"
          ]
        }
      },
      "required": [
        "displayName",
        "email",
        "role"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Almacena la información del perfil del usuario."
    }
  }
}